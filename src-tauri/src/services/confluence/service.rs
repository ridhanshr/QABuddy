//! Confluence service — held in app state. P3 wires connection testing + page
//! access; this module (P5b) adds page preview, attachment upload, page update,
//! Jira server-id detection, table generation, and Confluence-entry parsing.
//!
//! The HTML storage-format manipulation (XHTML tables, TOC macros, generated
//! markers) mirrors the Electron `confluence-service.ts` + `table-formatter.ts`
//! behaviour, ported to Rust regex.

use base64::Engine as _;
use serde_json::Value;

use crate::models::app_config::ConfluenceConfig;
use crate::models::jira::ConfluenceTestImportEntry;
use crate::models::misc::{
    ConfluencePreviewResult, ParseConfluenceEntriesOptions, ParseConfluenceEntriesResult,
    SyncToConfluencePayload, SyncToConfluenceResult,
};
use crate::services::confluence::client::ConfluenceClient;
use crate::services::error::Result;
use crate::services::http::normalize_url;

pub struct ConfluenceService;

impl ConfluenceService {
    pub fn new() -> Self {
        Self
    }

    fn client(&self, config: &ConfluenceConfig) -> ConfluenceClient {
        ConfluenceClient::new(config)
    }

    /// Probe Confluence connectivity; returns a status string.
    pub async fn test_connection(&self, config: &ConfluenceConfig) -> Result<String> {
        self.client(config).validate_connection().await
    }

    /// Fetch a raw Confluence page document.
    pub async fn get_page(&self, config: &ConfluenceConfig, page_id: &str) -> Result<Value> {
        self.client(config).get_page(page_id).await
    }

    /// Resolve a user-facing Confluence URL to a page document.
    pub async fn get_page_by_url(&self, config: &ConfluenceConfig, url: &str) -> Result<Value> {
        self.client(config).get_page_by_url(url).await
    }

    /// List pages for a space key or ancestor/page id. Used by RAG indexing.
    pub async fn list_pages(&self, config: &ConfluenceConfig, space_or_page_id: &str) -> Result<Vec<Value>> {
        self.client(config).list_pages(space_or_page_id).await
    }

    pub async fn get_attachments(&self, config: &ConfluenceConfig, page_id: &str) -> Result<Vec<Value>> {
        self.client(config).get_attachments(page_id).await
    }

    pub async fn download_attachment(&self, config: &ConfluenceConfig, download_path: &str) -> Result<Vec<u8>> {
        self.client(config).download_attachment(download_path).await
    }

    /// Lightweight page preview: title, storage body, version.
    pub async fn get_page_preview(
        &self,
        config: &ConfluenceConfig,
        page_id: &str,
    ) -> Result<(String, String, u32)> {
        let page = self.get_page(config, page_id).await?;
        let title = page["title"].as_str().unwrap_or("").to_string();
        let content = page["body"]["storage"]["value"]
            .as_str()
            .unwrap_or("")
            .to_string();
        let version = page["version"]["number"].as_u64().unwrap_or(1) as u32;
        Ok((title, content, version))
    }

    /// Resolve the Jira application-link server id from Confluence applinks.
    pub async fn detect_jira_server_id(&self, config: &ConfluenceConfig) -> Result<Option<String>> {
        Ok(self.client(config).detect_jira_server_id().await)
    }

    pub async fn upload_attachment(
        &self,
        config: &ConfluenceConfig,
        page_id: &str,
        filename: &str,
        data: Vec<u8>,
    ) -> Result<Value> {
        self.client(config)
            .upload_attachment(page_id, filename, data)
            .await
    }

    pub async fn update_page(
        &self,
        config: &ConfluenceConfig,
        page_id: &str,
        title: &str,
        content: &str,
        version: u32,
    ) -> Result<Value> {
        self.client(config)
            .update_page(page_id, title, content, version)
            .await
    }

    // ── Storage-format helpers ──────────────────────────────────────────

    /// Strip only our own "Generated by QA Buddy" trailer markers.
    /// Does NOT touch TOC macros, h1, or any other page content — those belong
    /// to the page owner and must be preserved exactly as fetched.
    fn strip_generated_markers(content: &str) -> String {
        let gen = regex::Regex::new(
            r#"(?i)\s*<p>\s*Generated by QA Buddy\s*[-—]\s*[^<]+</p>\s*"#,
        )
        .unwrap();
        let gen_simple = regex::Regex::new(
            r#"(?i)\s*<p>\s*Generated by QA Buddy\s*</p>\s*"#,
        )
        .unwrap();
        let mut out = gen.replace_all(content, "").to_string();
        out = gen_simple.replace_all(&out, "").to_string();
        out
    }

    /// Map category value from QA Buddy to Confluence display label.
    fn category_label(category: &str) -> &'static str {
        match category.trim().to_uppercase().as_str() {
            "TC_REGRESSION" | "REGRESSION" | "REGRESI" => "Regresi",
            "TC_UNHAPPY" | "UNHAPPY" | "NEGATIVE" => "Negative",
            "TC_HAPPY" | "HAPPY" | "POSITIVE" => "Positive",
            _ => "Positive",
        }
    }

    /// Build the HTML for a single Result cell — plain text, no colour.
    fn result_cell_html(result: &str) -> String {
        let label = match result.trim().to_uppercase().as_str() {
            "PASS" | "PASSED" => "Passed",
            "FAIL" | "FAILED" => "Failed",
            other if other.is_empty() => "",
            _ => result.trim(),
        };
        if label.is_empty() {
            String::new()
        } else {
            format!("<p>{}</p>", escape_html(label))
        }
    }

    /// Build the Screen Capture cell HTML: one Expand macro containing all images.
    fn screen_capture_cell_html(
        entry_id: &str,
        images: &[serde_json::Value],
        uploaded: &std::collections::HashMap<(String, String), String>,
    ) -> String {
        let mut sorted = images.to_vec();
        sorted.sort_by_key(|img| img["order"].as_u64().unwrap_or(0));

        let mut inner = String::new();
        for img in &sorted {
            let orig_name = img["name"].as_str().unwrap_or("").to_string();
            let key = (entry_id.to_string(), orig_name);
            if let Some(uploaded_name) = uploaded.get(&key) {
                inner.push_str(&format!(
                    r#"<p><ac:image ac:width="1200"><ri:attachment ri:filename="{}" /></ac:image></p>"#,
                    escape_html(uploaded_name)
                ));
            }
        }

        if inner.is_empty() {
            return String::new();
        }

        // All images in one Expand macro
        format!(
            r#"<ac:structured-macro ac:name="expand"><ac:parameter ac:name="title">Click here to expand...</ac:parameter><ac:rich-text-body>{}</ac:rich-text-body></ac:structured-macro>"#,
            inner
        )
    }

    /// Update existing vertical-card tables in `content` in-place.
    ///
    /// Strategy: scan for every `<table>…</table>` block; if it is a vertical
    /// table whose "No. Test Case" row value matches one of the payload entries,
    /// replace the value cells for all known fields with the new data.
    /// Returns the mutated content and a set of entry IDs that were matched.
    fn patch_existing_tables(
        content: &str,
        entries: &[serde_json::Value],
        uploaded: &std::collections::HashMap<(String, String), String>,
    ) -> (String, std::collections::HashSet<String>) {
        // Build lookup: composite key (testCaseNo + "|" + functionName) → entry.
        // Using only testCaseNo would cause false matches when multiple sub-sections
        // each restart numbering from TC001.
        let mut by_tc: std::collections::HashMap<String, &serde_json::Value> =
            std::collections::HashMap::new();
        for entry in entries {
            let tc  = entry["testCaseNo"].as_str().unwrap_or("").trim().to_lowercase();
            let fun = entry["functionName"].as_str().unwrap_or("").trim().to_lowercase();
            if !tc.is_empty() {
                // Primary key: tc + function (exact match across sub-sections)
                by_tc.insert(format!("{tc}|{fun}"), entry);
                // Fallback key: tc alone (used only if no function match found — see below)
                by_tc.entry(tc).or_insert(entry);
            }
        }

        let mut matched_ids: std::collections::HashSet<String> = std::collections::HashSet::new();

        // Collect all table positions first, then build result in one forward pass
        // so we never need offset tracking (which breaks when replacements shrink content).
        let table_positions = iter_table_positions(content);

        // For each table position, decide what to emit: original or patched
        struct Patch { tbl_start: usize, tbl_end: usize, replacement: String, entry_id: String }
        let mut patches: Vec<Patch> = Vec::new();

        for (tbl_start, tbl_end) in table_positions {
            let original_table = &content[tbl_start..tbl_end];

            if !is_vertical_table(original_table) {
                continue;
            }

            let tc_val = extract_vertical_cell(original_table, "no. test case")
                .or_else(|| extract_vertical_cell(original_table, "no test case"))
                .or_else(|| extract_vertical_cell(original_table, "no. tc"))
                .unwrap_or_default();
            let tc_key = tc_val.trim().to_lowercase();
            if tc_key.is_empty() { continue; }

            let fun_val = extract_vertical_cell(original_table, "function")
                .or_else(|| extract_vertical_cell(original_table, "function name"))
                .unwrap_or_default();
            let fun_key = fun_val.trim().to_lowercase();

            // Try composite key first; fall back to tc-only if function not present
            let composite = format!("{tc_key}|{fun_key}");
            let entry = if let Some(e) = by_tc.get(&composite) {
                e
            } else if !fun_key.is_empty() {
                // No composite match — this table belongs to a different sub-section
                continue;
            } else if let Some(e) = by_tc.get(&tc_key) {
                e
            } else {
                continue;
            };

            let entry_id = entry["id"].as_str().unwrap_or("").to_string();
            let images = entry["images"].as_array().cloned().unwrap_or_default();

            let category_html = format!("<p>{}</p>", escape_html(Self::category_label(entry["category"].as_str().unwrap_or(""))));
            let replacements: Vec<(&str, String)> = vec![
                ("function",        format!("<p>{}</p>", escape_html(entry["functionName"].as_str().unwrap_or("")))),
                ("scenario",        format!("<p>{}</p>", escape_html(entry["scenario"].as_str().unwrap_or("")))),
                ("kategori",        category_html.clone()),
                ("category",        category_html),
                ("input data",      format!("<p>{}</p>", escape_html(entry["inputData"].as_str().unwrap_or("")))),
                ("steps",           text_to_list_html(entry["steps"].as_str().unwrap_or(""))),
                ("expected result", text_to_list_html(entry["expectedResult"].as_str().unwrap_or(""))),
                ("result",          Self::result_cell_html(entry["result"].as_str().unwrap_or(""))),
                ("screen capture",  Self::screen_capture_cell_html(&entry_id, &images, uploaded)),
            ];

            let mut new_table = original_table.to_string();
            for (label, new_value) in &replacements {
                new_table = replace_vertical_cell_value(&new_table, label, new_value);
            }

            if new_table != original_table {
                patches.push(Patch { tbl_start, tbl_end, replacement: new_table, entry_id });
            }
        }

        // Build the final string in a single forward pass over the original content
        let mut result = String::with_capacity(content.len() + 1024);
        let mut cursor = 0usize;
        for patch in &patches {
            result.push_str(&content[cursor..patch.tbl_start]);
            result.push_str(&patch.replacement);
            cursor = patch.tbl_end;
            matched_ids.insert(patch.entry_id.clone());
        }
        result.push_str(&content[cursor..]);

        (result, matched_ids)
    }

    /// Generate vertical-card XHTML tables for entries that had no match in
    /// the existing page content (append-only path).
    pub fn generate_vertical_tables(
        entries: &[serde_json::Value],
        uploaded: &std::collections::HashMap<(String, String), String>,
    ) -> String {
        let mut html = String::new();
        for entry in entries {
            let id            = entry["id"].as_str().unwrap_or("").to_string();
            let test_case_no  = entry["testCaseNo"].as_str().unwrap_or("").to_string();
            let function_name = entry["functionName"].as_str().unwrap_or("").to_string();
            let scenario      = entry["scenario"].as_str().unwrap_or("").to_string();
            let category      = Self::category_label(entry["category"].as_str().unwrap_or(""));
            let input_data    = entry["inputData"].as_str().unwrap_or("").to_string();
            let steps         = entry["steps"].as_str().unwrap_or("").to_string();
            let expected      = entry["expectedResult"].as_str().unwrap_or("").to_string();
            let result        = entry["result"].as_str().unwrap_or("").to_string();
            let images        = entry["images"].as_array().cloned().unwrap_or_default();

            let screen_capture_html = Self::screen_capture_cell_html(&id, &images, uploaded);
            let result_html         = Self::result_cell_html(&result);

            html.push_str(r#"<table class="wrapped confluenceTable"><colgroup><col style="width: 200px;"/><col/></colgroup><tbody>"#);
            html.push_str(&format!("<tr><th class=\"confluenceTh\">No. Test Case</th><td class=\"confluenceTd\"><p>{}</p></td></tr>", escape_html(&test_case_no)));
            html.push_str(&format!("<tr><th class=\"confluenceTh\">Function</th><td class=\"confluenceTd\"><p>{}</p></td></tr>", escape_html(&function_name)));
            html.push_str(&format!("<tr><th class=\"confluenceTh\">Scenario</th><td class=\"confluenceTd\"><p>{}</p></td></tr>", escape_html(&scenario)));
            html.push_str(&format!("<tr><th class=\"confluenceTh\">Kategori</th><td class=\"confluenceTd\"><p>{}</p></td></tr>", escape_html(category)));
            html.push_str(&format!("<tr><th class=\"confluenceTh\">Input Data</th><td class=\"confluenceTd\"><p>{}</p></td></tr>", escape_html(&input_data)));
            html.push_str(&format!("<tr><th class=\"confluenceTh\">Steps</th><td class=\"confluenceTd\">{}</td></tr>", text_to_list_html(&steps)));
            html.push_str(&format!("<tr><th class=\"confluenceTh\">Expected Result</th><td class=\"confluenceTd\">{}</td></tr>", text_to_list_html(&expected)));
            html.push_str(&format!("<tr><th class=\"confluenceTh\">Result</th><td class=\"confluenceTd\">{}</td></tr>", result_html));
            html.push_str(&format!("<tr><th class=\"confluenceTh\">Screen Capture</th><td class=\"confluenceTd\">{}</td></tr>", screen_capture_html));
            html.push_str("</tbody></table><p></p>");
        }
        html
    }

    /// Generate a single XHTML `<table>` from import entries (one row each).
    /// Columns: TestCase No | Function Name | Scenario | Steps | Expected Result.
    /// Kept for parse roundtrip tests only — sync now uses generate_vertical_tables.
    pub fn generate_single_table(entries: &[ConfluenceTestImportEntry]) -> String {
        let mut html = String::from(
            "<table class=\"wrapped confluenceTable\"><colgroup><col/><col/><col/><col/><col/></colgroup>",
        );
        html.push_str("<tbody>");
        html.push_str(
            "<tr><th class=\"confluenceTh\">TestCase No</th><th class=\"confluenceTh\">Function Name</th><th class=\"confluenceTh\">Scenario</th><th class=\"confluenceTh\">Steps</th><th class=\"confluenceTh\">Expected Result</th></tr>",
        );
        for entry in entries {
            html.push_str(&format!(
                "<tr><td class=\"confluenceTd\">{}</td><td class=\"confluenceTd\">{}</td><td class=\"confluenceTd\">{}</td><td class=\"confluenceTd\">{}</td><td class=\"confluenceTd\">{}</td></tr>",
                escape_html(&entry.test_case_no),
                escape_html(&entry.function_name),
                escape_html(&entry.scenario),
                escape_html(&entry.steps),
                escape_html(&entry.expected_result),
            ));
        }
        html.push_str("</tbody></table>");
        html
    }

    /// Count how many existing import-row tables already exist in the content
    /// (heuristic: tables with the QA Buddy column header).
    fn count_existing_entries(content: &str) -> usize {
        let re = regex::Regex::new(r"(?i)<tr>\s*<th[^>]*>\s*TestCase No").unwrap();
        re.find_iter(content).count()
    }

    // ── High-level operations ───────────────────────────────────────────

    /// Build a preview of the sync: current title/version, generated tables,
    /// and entry/existing-entry counts. Does not write to Confluence.
    pub async fn preview_sync(
        &self,
        config: &ConfluenceConfig,
        page_id: &str,
        entries: &[Value],
    ) -> Result<ConfluencePreviewResult> {
        let (title, content, version) = self.get_page_preview(config, page_id).await?;
        let import_entries: Vec<ConfluenceTestImportEntry> = entries
            .iter()
            .filter_map(|e| serde_json::from_value::<ConfluenceTestImportEntry>(e.clone()).ok())
            .collect();
        let tables = Self::generate_single_table(&import_entries);
        let existing = Self::count_existing_entries(&content);
        Ok(ConfluencePreviewResult {
            current_title: title,
            current_version: version,
            generated_tables: tables,
            entry_count: import_entries.len() as u32,
            existing_entry_count: existing as u32,
        })
    }

    /// Sync entries into a Confluence page.
    ///
    /// If the page already has vertical-card tables whose "No. Test Case" value
    /// matches a payload entry, those tables are updated **in-place** (no new
    /// table is created).  Entries with no matching table are appended at the end.
    /// Images are uploaded as attachments and referenced in a single Expand macro.
    pub async fn sync_to_confluence(
        &self,
        config: &ConfluenceConfig,
        page_id: &str,
        payload: &SyncToConfluencePayload,
    ) -> Result<SyncToConfluenceResult> {
        let (title, raw_content, version) = self.get_page_preview(config, page_id).await?;
        let content = sanitize_confluence_storage_html(&raw_content);

        let entry_count = payload.entries.len() as u32;

        // ── 1. Upload all images and build lookup map ────────────────────
        let mut uploaded: std::collections::HashMap<(String, String), String> =
            std::collections::HashMap::new();
        let mut total_images = 0u32;

        for entry in &payload.entries {
            let entry_id = entry["id"].as_str().unwrap_or("").to_string();
            let images = entry["images"].as_array().cloned().unwrap_or_default();
            let mut sorted = images.clone();
            sorted.sort_by_key(|img| img["order"].as_u64().unwrap_or(0));

            for img in &sorted {
                let orig_name = img["name"].as_str().unwrap_or("attachment.png").to_string();
                let data_uri  = img["data"].as_str().unwrap_or("");
                if data_uri.is_empty() { continue; }

                let base64_data = if let Some(pos) = data_uri.find(',') {
                    &data_uri[pos + 1..]
                } else {
                    data_uri
                };
                let bytes = match base64::engine::general_purpose::STANDARD.decode(base64_data) {
                    Ok(b) => b,
                    Err(_) => continue,
                };

                let safe_entry = entry_id.replace(['/', '\\', ' '], "_");
                let upload_name = format!("{safe_entry}_{orig_name}");
                if self.upload_attachment(config, page_id, &upload_name, bytes).await.is_ok() {
                    uploaded.insert((entry_id.clone(), orig_name), upload_name);
                    total_images += 1;
                }
            }
        }

        // ── 3. Patch existing tables in-place ────────────────────────────
        let (mut content, matched_ids) =
            Self::patch_existing_tables(&content, &payload.entries, &uploaded);

        // ── 4. Append tables for entries not found in existing content ───
        let new_entries: Vec<&serde_json::Value> = payload
            .entries
            .iter()
            .filter(|e| {
                let id = e["id"].as_str().unwrap_or("");
                !matched_ids.contains(id)
            })
            .collect();

        if !new_entries.is_empty() {
            // Remove the "Generated by QA Buddy" trailer if present so we append before it
            content = Self::strip_generated_markers(&content);
            let new_tables = Self::generate_vertical_tables(
                &new_entries.into_iter().cloned().collect::<Vec<_>>(),
                &uploaded,
            );
            content.push_str(&new_tables);
        }

        // Always refresh the trailer
        content = Self::strip_generated_markers(&content);
        content.push_str("\n<p>Generated by QA Buddy</p>");

        // ── 5. Repair any unclosed inline tags left over from the original page ─
        let content = close_unclosed_inline_tags(&content);

        // ── 6. Write back to Confluence ──────────────────────────────────
        let updated = self.update_page(config, page_id, &title, &content, version).await?;
        let page_url = updated
            .get("_links")
            .and_then(|l| l.get("base"))
            .and_then(|b| b.as_str())
            .map(|b| format!(
                "{}/pages/viewpage.action?pageId={page_id}",
                normalize_url(b)
            ))
            .unwrap_or_else(|| format!("/pages/viewpage.action?pageId={page_id}"));

        Ok(SyncToConfluenceResult {
            page_title: title,
            page_url,
            entry_count,
            image_count: total_images,
            attachment_count: total_images,
        })
    }

    /// Parse a Confluence page's storage body into import entries. Each
    /// `<table>` whose first row has header cells is treated as a set of test
    /// rows; columns are mapped to ConfluenceTestImportEntry fields by header.
    pub async fn parse_confluence_entries(
        &self,
        config: &ConfluenceConfig,
        page_id: &str,
        _options: &ParseConfluenceEntriesOptions,
    ) -> Result<ParseConfluenceEntriesResult> {
        let (title, content, _version) = match self.get_page_preview(config, page_id).await {
            Ok(t) => (Some(t.0), t.1, t.2),
            Err(e) => {
                return Ok(ParseConfluenceEntriesResult {
                    page_id: page_id.to_string(),
                    page_title: String::new(),
                    content_loaded: false,
                    entries: vec![],
                    jira_server_id: None,
                    error: Some(e.to_string()),
                    debug: None,
                });
            }
        };
        let mut entries = parse_tables_to_entries(&content);
        let jira_server_id = self.client(config).detect_jira_server_id().await;

        // Download screen capture attachments for entries that need them
        let needs_images = entries.iter().any(|e| !e.screen_capture_filenames.is_empty());
        if needs_images {
            // Fetch all page attachments once
            let all_attachments = self.get_attachments(config, page_id).await.unwrap_or_default();
            // Build filename → download_path map
            let att_map: std::collections::HashMap<String, String> = all_attachments
                .iter()
                .filter_map(|a| {
                    let name = a["title"].as_str()?.to_string();
                    let path = a["_links"]["download"].as_str()?.to_string();
                    Some((name, path))
                })
                .collect();

            for entry in entries.iter_mut() {
                if entry.screen_capture_filenames.is_empty() {
                    continue;
                }
                let mut images: Vec<crate::models::jira::ConfluenceEntryImage> = Vec::new();
                for (order, filename) in entry.screen_capture_filenames.iter().enumerate() {
                    if let Some(download_path) = att_map.get(filename) {
                        match self.download_attachment(config, download_path).await {
                            Ok(bytes) => {
                                let ext = filename.rsplit('.').next().unwrap_or("png").to_lowercase();
                                let mime = match ext.as_str() {
                                    "jpg" | "jpeg" => "image/jpeg",
                                    "gif" => "image/gif",
                                    "webp" => "image/webp",
                                    _ => "image/png",
                                };
                                let b64 = base64_encode(&bytes);
                                images.push(crate::models::jira::ConfluenceEntryImage {
                                    id: uuid::Uuid::new_v4().to_string(),
                                    name: filename.clone(),
                                    data: format!("data:{mime};base64,{b64}"),
                                    order: order + 1,
                                    note: String::new(),
                                });
                            }
                            Err(_) => {} // skip failed downloads silently
                        }
                    }
                }
                entry.images = images;
            }
        }

        Ok(ParseConfluenceEntriesResult {
            page_id: page_id.to_string(),
            page_title: title.unwrap_or_default(),
            content_loaded: true,
            entries,
            jira_server_id,
            error: None,
            debug: None,
        })
    }
}

// ── free helpers ────────────────────────────────────────────────────────

fn base64_encode(data: &[u8]) -> String {
    base64::engine::general_purpose::STANDARD.encode(data)
}

/// Extract the plain-text value of a named field row in a vertical-card table.
/// Exact case-insensitive match on the trimmed label text.
fn extract_vertical_cell(table_html: &str, field: &str) -> Option<String> {
    for (_rs, _re, inner_start, inner_end) in iter_tr_positions(table_html) {
        let row_inner = &table_html[inner_start..inner_end];
        let cells = split_row_cells(row_inner);
        if cells.len() < 2 { continue; }
        let label = strip_inner_tags(&cells[0]).trim().to_lowercase();
        if label == field {
            return Some(strip_inner_tags(&cells[1]).trim().to_string());
        }
    }
    None
}

/// Replace the value cell (second `<td>`) of a named field row in a vertical-card table.
/// Exact case-insensitive match on the trimmed label text of the first cell.
///
/// Uses depth-tracking iterators (not naive regex) so nested tables and AC macros
/// that contain `</tr>` or `</td>` inside them are handled correctly.
fn replace_vertical_cell_value(table_html: &str, field: &str, new_value: &str) -> String {
    for (_row_start, _row_end, row_inner_start, row_inner_end)
        in iter_tr_positions(table_html)
    {
        let row_inner = &table_html[row_inner_start..row_inner_end];
        let cells = split_row_cells(row_inner);
        if cells.len() < 2 { continue; }

        let label = strip_inner_tags(&cells[0]).trim().to_lowercase();
        if label != field { continue; }

        // Found the right row — now splice the new value into the second cell.
        if let Some((cell_open, inner_start, inner_end, cell_end))
            = find_second_cell_positions(row_inner)
        {
            // cell positions are all relative to row_inner
            let open_tag  = &row_inner[cell_open..inner_start];
            let close_tag = &row_inner[inner_end..cell_end];
            let new_row_inner = format!(
                "{}{}{}{}{}",
                &row_inner[..cell_open],
                open_tag,
                new_value,
                close_tag,
                &row_inner[cell_end..],
            );
            let mut result = table_html.to_string();
            result.replace_range(row_inner_start..row_inner_end, &new_row_inner);
            return result;
        }
        break; // found the row but cell parse failed — leave unchanged
    }

    table_html.to_string()
}

/// Iterate top-level `<table>…</table>` blocks in `html` using depth tracking,
/// yielding `(start, end)` byte positions. Nested tables (inside macros or cells)
/// are skipped — only the outermost `<table>` boundaries are returned.
fn iter_table_positions(html: &str) -> Vec<(usize, usize)> {
    let open_re  = regex::Regex::new(r"(?i)<table\b[^>]*>").unwrap();
    let close_re = regex::Regex::new(r"(?i)</table>").unwrap();
    let len = html.len();
    let mut results = Vec::new();
    let mut pos = 0usize;

    while pos < len {
        let Some(open_m) = open_re.find(&html[pos..]) else { break };
        let tbl_start = pos + open_m.start();
        let mut depth = 1i32;
        let mut cur = pos + open_m.end();

        while cur < len && depth > 0 {
            let rest = &html[cur..];
            let next_open  = open_re.find(rest).map(|m| m.start());
            let next_close = close_re.find(rest).map(|m| (m.start(), m.end()));
            match (next_open, next_close) {
                (Some(o), Some((c, _))) if o < c => {
                    depth += 1;
                    cur += open_re.find(rest).unwrap().end();
                }
                (_, Some((_, ce))) => {
                    depth -= 1;
                    cur += ce;
                    if depth == 0 {
                        results.push((tbl_start, cur));
                        pos = cur;
                        break;
                    }
                }
                _ => { pos = len; break; }
            }
        }
        if depth > 0 { break; }
    }
    results
}

/// Iterate `<tr>…</tr>` blocks in `html`, yielding
/// `(row_start, row_end, inner_start, inner_end)` where the inner span
/// does **not** include the opening `<tr…>` or closing `</tr>` tag.
/// Depth-tracks nested `<tr>` so that tables-within-tables are handled
/// (Confluence Expand macros can embed arbitrary content).
fn iter_tr_positions(html: &str) -> Vec<(usize, usize, usize, usize)> {
    let open_re  = regex::Regex::new(r"(?i)<tr\b[^>]*>").unwrap();
    let close_re = regex::Regex::new(r"(?i)</tr>").unwrap();
    let len = html.len();
    let mut results = Vec::new();
    let mut pos = 0usize;

    while pos < len {
        let Some(open_m) = open_re.find(&html[pos..]) else { break };
        let tr_start      = pos + open_m.start();
        let inner_start   = pos + open_m.end();
        let mut depth = 1i32;
        let mut cur = inner_start;

        while cur < len && depth > 0 {
            let rest = &html[cur..];
            let next_open  = open_re.find(rest).map(|m| m.start());
            let next_close = close_re.find(rest).map(|m| m.start());
            match (next_open, next_close) {
                (Some(o), Some(c)) if o < c => {
                    depth += 1;
                    cur += open_re.find(rest).unwrap().end();
                }
                (_, Some(c)) => {
                    depth -= 1;
                    let close_end = cur + close_re.find(rest).unwrap().end();
                    if depth == 0 {
                        results.push((tr_start, close_end, inner_start, cur + c));
                        cur = close_end;
                        pos = cur;
                        break;
                    }
                    cur = close_end;
                }
                _ => { pos = len; break; }
            }
        }
        if depth > 0 { break; }
    }
    results
}

/// Within a row's inner HTML find the second `<t[dh]…>…</t[dh]>` cell using
/// depth-tracking, and return `(cell_open_start, inner_start, inner_end, cell_end)`.
/// All positions are relative to `row_inner`.
fn find_second_cell_positions(row_inner: &str) -> Option<(usize, usize, usize, usize)> {
    let open_re  = regex::Regex::new(r"(?i)<t[dh]\b[^>]*>").unwrap();
    let close_re = regex::Regex::new(r"(?i)</t[dh]>").unwrap();
    let len = row_inner.len();
    let mut cell_idx = 0usize;
    let mut pos = 0usize;

    while pos < len {
        let Some(open_m) = open_re.find(&row_inner[pos..]) else { break };
        let cell_open_start = pos + open_m.start();
        let inner_start     = pos + open_m.end();
        let mut depth = 1i32;
        let mut cur = inner_start;

        while cur < len && depth > 0 {
            let rest = &row_inner[cur..];
            let next_open  = open_re.find(rest).map(|m| m.start());
            let next_close = close_re.find(rest).map(|m| m.start());
            match (next_open, next_close) {
                (Some(o), Some(c)) if o < c => {
                    depth += 1;
                    cur += open_re.find(rest).unwrap().end();
                }
                (_, Some(c)) => {
                    depth -= 1;
                    let close_end = cur + close_re.find(rest).unwrap().end();
                    if depth == 0 {
                        cell_idx += 1;
                        if cell_idx == 2 {
                            return Some((cell_open_start, inner_start, cur + c, close_end));
                        }
                        pos = close_end;
                        break;
                    }
                    cur = close_end;
                }
                _ => return None,
            }
        }
        if depth > 0 { break; }
    }
    None
}

/// Sanitize existing Confluence page content so that every `<td>`/`<th>` whose
/// content starts with bare inline elements (`<strong>`, `<span>`, `<em>`, `<a>`,
/// `<code>`, etc.) — i.e. NOT already wrapped in a block-level element — gets
/// the whole cell content wrapped in a `<p>…</p>`.
///
/// This is needed because old content (created before our XHTML fix) may have
/// cells like `<td><strong>PASS</strong></td>` which Confluence rejects when we
/// write the page back.
fn sanitize_td_inline_content(html: &str) -> String {
    // Inline-only tags that need wrapping when they appear at the top of a cell
    let inline_tag_re = regex::Regex::new(
        r"(?i)^<(strong|em|span|a|code|s|u|sub|sup|b|i)\b"
    ).unwrap();
    // Block-level tags — if the content starts with one of these, no wrapping needed
    let block_tag_re = regex::Regex::new(
        r"(?i)^<(p|ol|ul|li|h[1-6]|table|tbody|tr|th|td|div|ac:|ri:|blockquote|pre)\b"
    ).unwrap();

    let open_re  = regex::Regex::new(r"(?i)<t[dh]\b[^>]*>").unwrap();
    let close_re = regex::Regex::new(r"(?i)</t[dh]>").unwrap();
    let len = html.len();
    let mut result = String::with_capacity(html.len() + 256);
    let mut pos = 0usize;

    while pos < len {
        let Some(open_m) = open_re.find(&html[pos..]) else {
            result.push_str(&html[pos..]);
            break;
        };
        // Copy everything up to and including the opening tag
        let _open_abs_start = pos + open_m.start();
        let open_abs_end    = pos + open_m.end();
        result.push_str(&html[pos..open_abs_end]);

        // Depth-track to find the matching close tag
        let inner_start = open_abs_end;
        let mut depth = 1i32;
        let mut cur = inner_start;
        let mut inner_end = inner_start;
        let mut cell_end = inner_start;

        while cur < len && depth > 0 {
            let rest = &html[cur..];
            let next_open  = open_re.find(rest).map(|m| m.start());
            let next_close = close_re.find(rest).map(|m| (m.start(), m.end()));
            match (next_open, next_close) {
                (Some(o), Some((c, _))) if o < c => {
                    depth += 1;
                    cur += open_re.find(rest).unwrap().end();
                }
                (_, Some((c, ce))) => {
                    depth -= 1;
                    if depth == 0 {
                        inner_end = cur + c;
                        cell_end  = cur + ce;
                        break;
                    }
                    cur += ce;
                }
                _ => { break; }
            }
        }

        if depth > 0 {
            // Unclosed cell — just copy the rest as-is
            result.push_str(&html[open_abs_end..]);
            break;
        }

        let inner = &html[inner_start..inner_end];
        let close_tag = &html[inner_end..cell_end];
        let trimmed = inner.trim();

        let needs_wrap = if trimmed.is_empty() {
            false
        } else if trimmed.starts_with('<') {
            // Starts with a tag — inline if matched by inline_tag_re, needs wrap
            // Block if matched by block_tag_re, no wrap needed
            inline_tag_re.is_match(trimmed) && !block_tag_re.is_match(trimmed)
        } else {
            // Starts with plain text — always needs a <p> wrapper
            true
        };

        if needs_wrap {
            result.push_str("<p>");
            result.push_str(trimmed);
            result.push_str("</p>");
        } else {
            result.push_str(inner);
        }
        result.push_str(close_tag);
        pos = cell_end;
    }

    result
}

/// Sanitize Confluence storage-format HTML fetched from an existing page so that
/// the content can be safely written back via the REST API without a 400 error.
///
/// Problems this handles:
/// 1. Unknown / non-Confluence XML tags (`<xml>`, `<o:p>`, `<w:...>`, `<v:...>`,
///    `<m:...>`, `<x:...>`, etc.) left over from pasted Office/Word content —
///    strip the tag itself but keep the inner text.
/// 2. Orphan close tags (`</div>`, `</xml>`, `</span>` etc.) that have no
///    matching open tag — remove them.
/// 3. Unclosed inline tags (`<strong>`, `<em>`, …) at block boundaries —
///    handled by `close_unclosed_inline_tags` which is called after this.
fn sanitize_confluence_storage_html(html: &str) -> String {
    // ── Step 1: strip Office/Word XML namespace tags entirely (open + close) ──
    // These are tags like <xml>, <o:p>, <w:sdtPr>, <v:shape>, <m:math> etc.
    // We remove the tag but keep any text content inside.
    // Also remove XML processing instructions: <?xml ...?> and <!...>
    let office_tag_re = regex::Regex::new(
        r"(?i)</?(?:xml|o:[a-z0-9]+|w:[a-z0-9]+|v:[a-z0-9]+|m:[a-z0-9]+|x:[a-z0-9]+)[^>]*/?>"
    ).unwrap();
    let pi_re = regex::Regex::new(r"(?i)<\?xml[^?]*\?>|<![A-Za-z][^>]*>").unwrap();
    let mut out = office_tag_re.replace_all(html, "").to_string();
    out = pi_re.replace_all(&out, "").to_string();

    // ── Step 2: track open/close counts for block-level tags and remove
    //    orphan close tags (close tags with no matching open tag) ──────────
    // We only track the tags that Confluence commonly complains about.
    const BLOCK_TAGS: &[&str] = &["div", "section", "article", "header", "footer", "nav", "aside"];
    out = remove_orphan_close_tags(&out, BLOCK_TAGS);

    out
}

/// Remove close tags (e.g. `</div>`) that have no corresponding open tag.
/// Uses a running depth counter: whenever a close tag is encountered with
/// depth == 0 (no matching open tag seen yet), that close tag is dropped.
fn remove_orphan_close_tags(html: &str, tags: &[&str]) -> String {
    let mut out = html.to_string();
    for tag in tags {
        let open_re  = regex::Regex::new(&format!(r"(?i)<{tag}\b[^>]*>")).unwrap();
        let close_re = regex::Regex::new(&format!(r"(?i)</{tag}>")).unwrap();

        // Build sorted event list: (pos, end, is_close)
        let mut events: Vec<(usize, usize, bool)> = Vec::new();
        for m in open_re.find_iter(&out)  { events.push((m.start(), m.end(), false)); }
        for m in close_re.find_iter(&out) { events.push((m.start(), m.end(), true));  }
        events.sort_by_key(|e| e.0);

        // Walk events; collect positions of orphan close tags to remove
        let mut depth = 0i32;
        let mut orphans: Vec<(usize, usize)> = Vec::new();
        for (pos, end, is_close) in &events {
            if *is_close {
                if depth == 0 {
                    orphans.push((*pos, *end));
                } else {
                    depth -= 1;
                }
            } else {
                depth += 1;
            }
        }

        if orphans.is_empty() { continue; }

        // Rebuild string skipping orphan ranges
        let mut result = String::with_capacity(out.len());
        let mut cursor = 0usize;
        for (start, end) in &orphans {
            result.push_str(&out[cursor..*start]);
            cursor = *end;
        }
        result.push_str(&out[cursor..]);
        out = result;
    }
    out
}

/// Remove orphan close tags for both block and inline tags.
/// An orphan close tag is one with no matching open tag before it in the document.
fn close_unclosed_inline_tags(html: &str) -> String {
    // Remove orphan close tags for all tags that Confluence's XHTML parser enforces.
    // Order matters: process outer tags before inner ones.
    const TAGS: &[&str] = &[
        "div", "section",
        "p",
        "strong", "em", "span", "b", "i", "s", "u", "code",
    ];
    remove_orphan_close_tags(html, TAGS)
}

/// Minimal HTML-escape for cell values pushed into storage format.
fn escape_html(s: &str) -> String {
    s.replace('&', "&amp;")
        .replace('<', "&lt;")
        .replace('>', "&gt;")
}

/// Convert a numbered-list string like "- 1. foo - 2. bar - 3. baz"
/// into a Confluence XHTML ordered list `<ol><li>foo</li>…</ol>`.
/// If the text doesn't look like a numbered list, wraps it in a plain `<p>`.
fn text_to_list_html(text: &str) -> String {
    // Split on the " - N." pattern (dash, space, digits, dot).
    // The leading "- 1." at the very start is also treated as a delimiter.
    let re = regex::Regex::new(r"(?:^|\s)-\s*\d+\.\s*").unwrap();
    let parts: Vec<&str> = re.split(text).map(|s| s.trim()).filter(|s| !s.is_empty()).collect();

    if parts.len() <= 1 {
        // No list structure detected — emit a plain paragraph.
        return format!("<p>{}</p>", escape_html(text.trim()));
    }

    let mut html = String::from("<ol>");
    for part in parts {
        html.push_str(&format!("<li>{}</li>", escape_html(part)));
    }
    html.push_str("</ol>");
    html
}

/// Normalize a field label from a vertical-format table row (left column).
fn normalise_field_label(raw: &str) -> String {
    strip_inner_tags(raw).trim().to_lowercase()
}

/// Detect whether a table uses the vertical (card) format used by Documentation Sync,
/// where each row is: | Field Label | Value | and field labels match known QA fields.
fn is_vertical_table(body: &str) -> bool {
    let row_re = regex::Regex::new(r"(?is)<tr\b[^>]*>([\s\S]*?)</tr>").unwrap();
    let cell_re = regex::Regex::new(r"(?is)<t[dh][^>]*>([\s\S]*?)</t[dh]>").unwrap();
    let known: &[&str] = &[
        "no. test case", "no test case", "no. tc", "function", "scenario",
        "kategori", "input data", "steps", "test steps", "expected result",
        "result", "screen capture",
    ];
    let mut matched = 0usize;
    let mut checked = 0usize;
    for row_cap in row_re.captures_iter(body) {
        let cells: Vec<String> = cell_re
            .captures_iter(&row_cap[1])
            .map(|c| normalise_field_label(&c[1]))
            .collect();
        if cells.len() >= 1 {
            checked += 1;
            let label = cells[0].as_str();
            if known.iter().any(|k| label.contains(k)) {
                matched += 1;
            }
        }
        if checked >= 6 {
            break;
        }
    }
    checked > 0 && matched >= 3
}

/// Extract a Jira issue key from a Confluence Jira macro in storage format.
/// e.g. <ac:structured-macro ac:name="jira"><ac:parameter ac:name="key">TRALOS-1</ac:parameter>...
/// Falls back to stripping all tags if no macro found.
fn extract_scenario_text(raw: &str) -> String {
    // Try to find a Jira macro key parameter
    let key_re = regex::Regex::new(
        r#"(?is)<ac:parameter\b[^>]*\bac:name\s*=\s*"key"[^>]*>([\s\S]*?)</ac:parameter>"#,
    )
    .unwrap();
    let keys: Vec<String> = key_re
        .captures_iter(raw)
        .map(|c| strip_inner_tags(&c[1]).trim().to_string())
        .filter(|s| !s.is_empty())
        .collect();
    if !keys.is_empty() {
        return keys.join(", ");
    }
    // Fallback: plain text
    strip_inner_tags(raw).trim().to_string()
}

/// Extract attachment filenames from a Confluence cell.
/// Handles both direct <ri:attachment ri:filename="..."/> and
/// filenames wrapped inside Expand macros or other nested structures.
/// Also handles single-quoted attribute values.
fn extract_attachment_filenames(raw: &str) -> Vec<String> {
    // Match ri:filename with double or single quotes
    let re = regex::Regex::new(
        r#"(?i)<ri:attachment\b[^>]*\bri:filename\s*=\s*(?:"([^"]+)"|'([^']+)')"#,
    )
    .unwrap();
    re.captures_iter(raw)
        .map(|c| {
            c.get(1)
                .or_else(|| c.get(2))
                .map(|m| m.as_str().to_string())
                .unwrap_or_default()
        })
        .filter(|s| !s.is_empty())
        .collect()
}

/// Split a table row's raw HTML into individual cell raw HTML contents.
/// Uses a simple open/close counter to handle nested tags inside cells.
fn split_row_cells(row_html: &str) -> Vec<String> {
    let mut cells = Vec::new();
    let open_re = regex::Regex::new(r"(?i)<(td|th)\b[^>]*>").unwrap();
    let close_re = regex::Regex::new(r"(?i)</(td|th)>").unwrap();
    let len = row_html.len();
    let mut pos = 0usize;

    while pos < len {
        let Some(open_cap) = open_re.find(&row_html[pos..]) else { break };
        let cell_start = pos + open_cap.end();
        let mut depth = 1i32;
        let mut cur = cell_start;
        while cur < len && depth > 0 {
            let rest = &row_html[cur..];
            let next_open = open_re.find(rest).map(|m| m.start());
            let next_close = close_re.find(rest).map(|m| m.start());
            match (next_open, next_close) {
                (Some(o), Some(c)) if o < c => {
                    depth += 1;
                    cur += open_re.find(rest).unwrap().end();
                }
                (_, Some(c)) => {
                    depth -= 1;
                    let close_end = cur + close_re.find(rest).unwrap().end();
                    if depth == 0 {
                        cells.push(row_html[cell_start..cur + c].to_string());
                        cur = close_end;
                        break;
                    }
                    cur = close_end;
                }
                _ => break,
            }
        }
        if cur <= cell_start { break; }
        pos = cur;
    }
    cells
}

/// Find the position of "Screen Capture" label row in the table body,
/// then extract ALL ri:attachment filenames from the rest of the table body
/// after that label — this handles Expand macros that contain </tr> inside them.
fn extract_screen_capture_filenames_from_body(body: &str) -> Vec<String> {
    // Find where the Screen Capture label appears (case-insensitive plain text)
    let lower = body.to_lowercase();
    let marker_pos = lower
        .find("screen capture")
        .or_else(|| lower.find("screenshot"))
        .or_else(|| lower.find("screen shot"));

    if let Some(pos) = marker_pos {
        // Search for ri:attachment anywhere after the marker in the full body
        extract_attachment_filenames(&body[pos..])
    } else {
        Vec::new()
    }
}

/// Parse a vertical-format table (one test case per table block).
/// Rows: | Field Label | Value |
fn parse_vertical_table(body: &str, counter: &mut usize) -> Option<ConfluenceTestImportEntry> {
    let mut test_case_no = String::new();
    let mut function_name = String::new();
    let mut scenario = String::new();
    let mut steps = String::new();
    let mut expected_result = String::new();
    let mut input_data = String::new();
    let mut found_any = false;

    for (_rs, _re, inner_start, inner_end) in iter_tr_positions(body) {
        let row_inner = &body[inner_start..inner_end];
        // Use depth-aware cell splitter to handle nested tags inside cells
        let raw_cells = split_row_cells(row_inner);
        if raw_cells.len() < 2 {
            continue;
        }
        let label = strip_inner_tags(&raw_cells[0]).trim().to_lowercase();
        let raw_value = &raw_cells[1];

        if label.contains("no. test case") || label.contains("no test case") || label.contains("no. tc") || label == "no tc" {
            test_case_no = strip_inner_tags(raw_value).trim().to_string();
            found_any = true;
        } else if label.contains("function") {
            function_name = strip_inner_tags(raw_value).trim().to_string();
            found_any = true;
        } else if label.contains("scenario") {
            scenario = extract_scenario_text(raw_value);
            found_any = true;
        } else if label.contains("input data") {
            input_data = strip_inner_tags(raw_value).trim().to_string();
            found_any = true;
        } else if label.contains("steps") || label.contains("langkah") {
            steps = strip_inner_tags(raw_value).trim().to_string();
            found_any = true;
        } else if label.contains("expected") || label.contains("hasil yang diharapkan") {
            expected_result = strip_inner_tags(raw_value).trim().to_string();
            found_any = true;
        }
    }

    // Extract screen capture filenames from the full table body (bypasses lazy </tr> truncation)
    // This correctly handles Expand macros that embed </tr> inside their body.
    let screen_capture_filenames = extract_screen_capture_filenames_from_body(body);

    if !found_any {
        return None;
    }

    let id = format!("tc-{}", counter);
    *counter += 1;
    Some(ConfluenceTestImportEntry {
        id,
        issue_key: String::new(),
        test_case_no,
        function_name,
        scenario,
        input_data,
        steps,
        expected_result,
        selected: true,
        screen_capture_filenames,
        images: Vec::new(), // populated later by parse_confluence_entries
    })
}

/// Parse every `<table>` in a storage body into import entries.
/// Supports two formats:
///   1. Horizontal: first row = headers (No. Test Case, Function, Scenario, Steps, Expected Result)
///   2. Vertical (card): each row = | Field Label | Value | — one test case per table
fn parse_tables_to_entries(content: &str) -> Vec<ConfluenceTestImportEntry> {
    let mut entries: Vec<ConfluenceTestImportEntry> = Vec::new();
    let table_re = regex::Regex::new(r"(?is)<table\b[^>]*>([\s\S]*?)</table>").unwrap();
    let mut counter = 1usize;
    for table_cap in table_re.captures_iter(content) {
        let body = &table_cap[1];

        // Detect vertical (card) format first
        if is_vertical_table(body) {
            if let Some(entry) = parse_vertical_table(body, &mut counter) {
                entries.push(entry);
            }
            continue;
        }

        // Horizontal format: header row + data rows
        let (header_map, has_header) = parse_header_map(body);
        let row_re = regex::Regex::new(r"(?is)<tr\b[^>]*>([\s\S]*?)</tr>").unwrap();
        let cell_re = regex::Regex::new(r"(?is)<t[dh][^>]*>([\s\S]*?)</t[dh]>").unwrap();
        for row_cap in row_re.captures_iter(body) {
            let row = &row_cap[1];
            // Skip header rows.
            if regex::Regex::new(r"(?i)<th\b").unwrap().is_match(row) {
                continue;
            }
            let cells: Vec<String> = cell_re
                .captures_iter(row)
                .map(|c| strip_inner_tags(&c[1]).trim().to_string())
                .collect();
            if cells.iter().all(|c| c.is_empty()) {
                continue;
            }
            let entry = build_entry(&cells, &header_map, has_header, &mut counter);
            entries.push(entry);
        }
    }
    entries
}

/// Map header text → column index. Returns (map, has_header).
fn parse_header_map(body: &str) -> (std::collections::HashMap<String, usize>, bool) {
    let mut map = std::collections::HashMap::new();
    let row_re = regex::Regex::new(r"(?is)<tr\b[^>]*>([\s\S]*?)</tr>").unwrap();
    let cell_re = regex::Regex::new(r"(?is)<th\b[^>]*>([\s\S]*?)</th>").unwrap();
    for row_cap in row_re.captures_iter(body) {
        let headers: Vec<String> = cell_re
            .captures_iter(&row_cap[1])
            .map(|c| normalise_header(&c[1]))
            .collect();
        if !headers.is_empty() {
            for (i, h) in headers.iter().enumerate() {
                map.insert(h.clone(), i);
            }
            return (map, true);
        }
    }
    (map, false)
}

fn normalise_header(raw: &str) -> String {
    let s = strip_inner_tags(raw).trim().to_lowercase();
    s.replace("test case no", "testcase")
        .replace("testcase no", "testcase")
        .replace("function name", "function")
        .replace("scenario", "scenario")
        .replace("steps", "steps")
        .replace("test steps", "steps")
        .replace("expected result", "expected")
        .replace("expected", "expected")
}

fn build_entry(
    cells: &[String],
    header_map: &std::collections::HashMap<String, usize>,
    has_header: bool,
    counter: &mut usize,
) -> ConfluenceTestImportEntry {
    let pick = |key: &str| -> String {
        header_map
            .get(key)
            .and_then(|i| cells.get(*i))
            .cloned()
            .unwrap_or_default()
    };
    let (test_case_no, function_name, scenario, steps, expected_result) = if has_header {
        (
            pick("testcase"),
            pick("function"),
            pick("scenario"),
            pick("steps"),
            pick("expected"),
        )
    } else {
        // Positional fallback: [No, Function, Scenario, Steps, Expected].
        (
            cells.first().cloned().unwrap_or_default(),
            cells.get(1).cloned().unwrap_or_default(),
            cells.get(2).cloned().unwrap_or_default(),
            cells.get(3).cloned().unwrap_or_default(),
            cells.get(4).cloned().unwrap_or_default(),
        )
    };
    let id = format!("tc-{}", counter);
    *counter += 1;
    ConfluenceTestImportEntry {
        id,
        issue_key: String::new(),
        scenario,
        steps,
        expected_result,
        function_name,
        test_case_no,
        input_data: String::new(),
        selected: true,
        screen_capture_filenames: Vec::new(),
        images: Vec::new(),
    }
}

/// Remove nested HTML tags inside a cell, collapse whitespace.
fn strip_inner_tags(raw: &str) -> String {
    let re = regex::Regex::new(r"(?is)<[^>]+>").unwrap();
    let ws = regex::Regex::new(r"\s+").unwrap();
    let decoded = raw
        .replace("&nbsp;", " ")
        .replace("&amp;", "&")
        .replace("&lt;", "<")
        .replace("&gt;", ">")
        .replace("&quot;", "\"")
        .replace("&#39;", "'");
    let stripped = re.replace_all(&decoded, " ");
    ws.replace_all(stripped.trim(), " ").to_string()
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn parse_tables_to_entries_positional() {
        let html = r#"<table><tbody>
            <tr><th>TestCase No</th><th>Function Name</th><th>Scenario</th><th>Steps</th><th>Expected Result</th></tr>
            <tr><td>TC-001</td><td>Login</td><td>User logs in</td><td>Click login</td><td>Dashboard shown</td></tr>
        </tbody></table>"#;
        let entries = parse_tables_to_entries(html);
        assert_eq!(entries.len(), 1);
        assert_eq!(entries[0].test_case_no, "TC-001");
        assert_eq!(entries[0].function_name, "Login");
        assert_eq!(entries[0].expected_result, "Dashboard shown");
    }

    #[test]
    fn generate_single_table_roundtrip() {
        let entries = vec![ConfluenceTestImportEntry {
            id: "tc-1".into(),
            issue_key: String::new(),
            scenario: "Login".into(),
            steps: "Click".into(),
            expected_result: "OK".into(),
            function_name: "Auth".into(),
            test_case_no: "TC-1".into(),
            input_data: String::new(),
            selected: true,
        }];
        let table = ConfluenceService::generate_single_table(&entries);
        assert!(table.contains("TC-1"));
        assert!(table.contains("Auth"));
    }

    #[test]
    fn strip_generated_markers_removes_gen_paragraph() {
        let html = "<p>hello</p>\n<p>Generated by QA Buddy — 2026-06-18</p>\n<p>world</p>";
        let cleaned = ConfluenceService::strip_generated_markers(html);
        assert!(!cleaned.contains("Generated by QA Buddy"));
        assert!(cleaned.contains("hello"));
        assert!(cleaned.contains("world"));
    }
}
